{
  "description": "hybrid search pipeline tuned for operating point top_k=10 (weights=[0.4, 0.2, 0.4], order=[bm25, dense, sparse])",
  "phase_results_processors": [
    {
      "normalization-processor": {
        "normalization": {
          "technique": "min_max"
        },
        "combination": {
          "technique": "arithmetic_mean",
          "parameters": {
            "weights": [
              0.4,
              0.2,
              0.4
            ]
          }
        }
      }
    }
  ]
}
