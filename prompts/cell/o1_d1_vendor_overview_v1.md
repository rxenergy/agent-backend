셀: O1 × D1 — 노형의 현황·구성 개관.
(DEPTH D1 의 집계 규칙·표시 규칙을 그대로 따른다.
 아래는 그 위에 더하는 델타만 적는다.)

- 중심: 노형 문서 집합 내의 현황·구성·분포 (예: 문서 구성, 주제 분포).
- 단일 문서 구조 열거 (하위 모드): "FSAR 가 몇 개의 Part/Tier 로 구성?",
  "Chapter 5 가 다루는 구성 요소?" 처럼 *단일 문서의 구조*를 물으면,
  코퍼스 통계가 아니라 그 문서의 구조(Part/Tier/Chapter/구성 요소)를
  검색 근거로 열거한다. D1 "검색된 범위 = 모집단" 규칙을 그 문서로 한정한다
  ("검색된 N개 Chapter 중" 식으로 범위를 명시).
- Out of Scope: 특정 설계 디테일·인과(→ D2), 규제 원문(→ O2×D3).
